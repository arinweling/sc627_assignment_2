# SC627 Assignment 2

## Setup

```bash
# from repo root
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Run

```bash
python3 week1_open_loop.py
```

The script saves the plot as `trajectories.png` in the repo folder.
